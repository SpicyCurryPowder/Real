<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>REAL . - Sources</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <header>
        <h1>REAL .</h1>
    </header>
    <main>
        <h1 class="feed-title">Sources</h1>
        <div class="news-container">
            <% likedNews.forEach(item => { %>
                <div class="news-item">
                    <h3><%= item.title %></h3>
                    <p><%= item.description %></p>
                    <p><strong>Source:</strong> <%= item.source %></p>
                    <button onclick="toggleLike('<%= item.id %>', '<%= item.title %>', '<%= item.description %>', '<%= item.source %>')" class="like-btn liked">
                        Unlike
                    </button>
                </div>
            <% }); %>
        </div>
    </main>
    <nav class="bottom-nav">
        <a href="/">Home</a>
        <a href="/verified-news">Verified News</a>
        <a href="/source-news" class="active">Sources</a>
    </nav>
    <script src="/script.js"></script>
</body>
</html>
